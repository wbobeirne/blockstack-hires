import React from 'react';
import './Header.scss';

export default class Header extends React.Component<{}> {
  public render() {
    return (
      <header className="Header">
        Header
      </header>
    );
  }
};
